// scripts/generate-static-data.mjs
// This script was removed as part of the migration away from Strapi.
// Article/content reads are now performed from Sanity (or static JSON fallbacks).

console.log('generate-static-data has been removed. Use Sanity or the static data files in src/data if needed.');
process.exit(0);
